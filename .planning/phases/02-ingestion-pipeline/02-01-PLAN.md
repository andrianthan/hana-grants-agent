---
phase: 02-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/scrapers/__init__.py
  - scripts/scrapers/handler.py
  - scripts/scrapers/base_scraper.py
  - scripts/scrapers/base_api_client.py
  - scripts/scrapers/api/__init__.py
  - scripts/scrapers/api/grants_ca_gov.py
  - scripts/scrapers/api/grants_gov.py
  - scripts/scrapers/api/propublica.py
  - scripts/scrapers/api/usaspending.py
  - scripts/scrapers/api/grantmakers_io.py
  - scripts/scrapers/requirements.txt
  - infrastructure/docker/scraper/Dockerfile
  - pytest.ini
  - scripts/tests/conftest.py
  - scripts/tests/test_api_parsers.py
autonomous: true
requirements: [INGEST-01, INFRA-04]

must_haves:
  truths:
    - "Each API parser returns a list of RawGrant objects with title, funder, description, source_url, source_id, and content_hash"
    - "grants.ca.gov parser fetches from CKAN datastore_search API with correct resource_id and handles text-typed date/amount fields"
    - "Grants.gov parser uses POST search2 endpoint with nonprofit eligibility filter and handles pagination via startRecordNum"
    - "Grantmakers.io is implemented as a Playwright scraper (not API) since no documented REST API exists"
    - "Scraper Docker image builds successfully with Playwright + Chromium only (not all browsers)"
    - "Lambda handler reads scraper_registry.json at runtime and dispatches to correct parser class"
  artifacts:
    - path: "scripts/scrapers/base_scraper.py"
      provides: "RawGrant dataclass with content_hash property, BaseScraper abstract base class"
      exports: ["RawGrant", "BaseScraper"]
    - path: "scripts/scrapers/handler.py"
      provides: "Lambda entry point that reads scraper_registry.json and dispatches"
      exports: ["handler"]
    - path: "scripts/scrapers/api/grants_ca_gov.py"
      provides: "grants.ca.gov CKAN API client"
      contains: "resource_id"
    - path: "infrastructure/docker/scraper/Dockerfile"
      provides: "Docker image for scraper Lambda"
      contains: "playwright install chromium"
  key_links:
    - from: "scripts/scrapers/handler.py"
      to: "scraper_registry.json"
      via: "json.load at runtime"
      pattern: "scraper_registry"
    - from: "scripts/scrapers/api/grants_ca_gov.py"
      to: "scripts/scrapers/base_api_client.py"
      via: "class inheritance"
      pattern: "class GrantsCaGov.*BaseApiClient"
---

<objective>
Create the scraper foundation: RawGrant data model, base classes, Lambda handler, all 5 API parsers, Dockerfile, pytest configuration, and unit tests with fixtures.

Purpose: Establishes the contract (RawGrant) and dispatch mechanism (handler.py) that all 17 scrapers build on. API parsers are deterministic and testable without Playwright, making them the ideal first deliverable.

Output: Working API parsers for grants.ca.gov, Grants.gov, ProPublica, USASpending, and Grantmakers.io (as Playwright scraper per research finding). Docker image definition. Test suite with fixtures. pytest.ini configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion-pipeline/02-CONTEXT.md
@.planning/phases/02-ingestion-pipeline/02-RESEARCH.md
@scraper_registry.json

<interfaces>
<!-- Phase 1 utilities that scrapers will use -->

From scripts/utils/config.py:
```python
EMBEDDING_DIMS = 1024
EMBEDDING_MODEL_ID = "amazon.titan-embed-text-v2:0"
AWS_REGION = "us-west-2"
HYDE_MODEL = "openai/gpt-4.1"
```

From scripts/utils/db.py:
```python
def get_connection(secret_arn: str, region: str = AWS_REGION) -> psycopg2.connection
```

From scripts/utils/embeddings.py:
```python
def get_embedding(text: str) -> list[float]
```

From scripts/init_db.py grants table schema:
```sql
CREATE TABLE IF NOT EXISTS grants (
    id SERIAL PRIMARY KEY,
    grant_id TEXT UNIQUE NOT NULL,
    title TEXT, funder TEXT, deadline DATE,
    funding_min INTEGER, funding_max INTEGER,
    geography TEXT, eligibility TEXT, description TEXT,
    program_area TEXT, population_served TEXT,
    relationship_req BOOLEAN DEFAULT FALSE,
    embedding vector(1024),
    source TEXT, raw_s3_key TEXT,
    content_hash TEXT NOT NULL,
    approval_status TEXT DEFAULT 'pending',
    skip_reason TEXT, score FLOAT, score_reasoning TEXT,
    score_flags JSONB, scored_by_profiles TEXT[],
    ingested_at TIMESTAMPTZ DEFAULT NOW(),
    scored_at TIMESTAMPTZ, updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(content_hash)
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create base classes, Lambda handler, pytest config, and test scaffolding</name>
  <files>
    scripts/scrapers/__init__.py
    scripts/scrapers/handler.py
    scripts/scrapers/base_scraper.py
    scripts/scrapers/base_api_client.py
    scripts/scrapers/api/__init__.py
    scripts/scrapers/requirements.txt
    infrastructure/docker/scraper/Dockerfile
    pytest.ini
    scripts/tests/__init__.py
    scripts/tests/conftest.py
  </files>
  <read_first>
    scraper_registry.json
    scripts/utils/config.py
    scripts/utils/db.py
    scripts/utils/embeddings.py
    scripts/ingest_documents.py
  </read_first>
  <behavior>
    - RawGrant("Test Grant", "Test Funder", "desc", None, "http://example.com", "test-id").content_hash returns SHA-256 of "Test Grant|Test Funder||desc"
    - RawGrant with same content produces same content_hash (deterministic)
    - BaseScraper.validate([grant_with_title, grant_without_title]) returns only the grant with title
    - handler dispatches to correct scraper class based on scraper_id from event payload
  </behavior>
  <action>
    Create the scraper foundation per D-10 (registry-driven dispatch):

    1. `scripts/scrapers/base_scraper.py`:
       - `RawGrant` dataclass with fields: title (str), funder (str), description (str), deadline (Optional[str]), source_url (str), source_id (str), raw_html (Optional[str] = None)
       - `content_hash` property: SHA-256 of f"{self.title}|{self.funder}|{self.deadline or ''}|{self.description}" per D-08
       - `BaseScraper` ABC with __init__(self, config: dict) storing scraper_id, url, source_type from config
       - Abstract method: `async def fetch_grants(self) -> list[RawGrant]`
       - Concrete method: `def validate(self, grants: list[RawGrant]) -> list[RawGrant]` filtering out grants missing title or description

    2. `scripts/scrapers/base_api_client.py`:
       - `BaseApiClient(BaseScraper)` adding httpx.AsyncClient setup
       - Helper: `async def _get_json(self, url, params=None, timeout=30) -> dict`
       - Helper: `async def _post_json(self, url, json_data=None, timeout=30) -> dict`
       - Rate limiting: simple asyncio.sleep between paginated requests

    3. `scripts/scrapers/handler.py`:
       - Lambda entry point: `def handler(event, context)`
       - Reads `scraper_registry.json` from same directory (bundled in Docker image)
       - Event payload: `{"scraper_id": "grants-ca-gov"}` -- looks up config from registry, instantiates correct class
       - Registry mapping dict: `SCRAPER_CLASSES = {"grants-ca-gov": GrantsCaGov, "grants-gov": GrantsGov, ...}`
       - Returns: `{"scraper_id": str, "grants_found": int, "grants": [dict]}`
       - Wraps in try/except, returns error dict on failure

    4. `scripts/scrapers/requirements.txt`:
       ```
       playwright==1.58.0
       playwright-stealth==2.0.2
       openai>=2.30.0,<3.0.0
       pydantic>=2.12.0,<3.0.0
       httpx>=0.28.0,<1.0.0
       psycopg2-binary>=2.9.10,<3.0.0
       pgvector>=0.3.6
       boto3>=1.42.0
       python-dotenv>=1.0.0
       awslambdaric
       ```

    5. `infrastructure/docker/scraper/Dockerfile`:
       - Base: `mcr.microsoft.com/playwright/python:v1.58.0-noble`
       - Multi-stage build per research pattern
       - Install ONLY chromium: `playwright install chromium` (not all browsers, per Pitfall 2)
       - FUNCTION_DIR="/function"
       - Copy scripts/scrapers/, scripts/utils/, scraper_registry.json into FUNCTION_DIR
       - Install awslambdaric + requirements.txt into FUNCTION_DIR
       - ENTRYPOINT: `python -m awslambdaric`
       - CMD: `scrapers.handler.handler`
       - Architecture: amd64 (per research recommendation, not arm64)

    6. `pytest.ini` (WARNING 3 fix — per RESEARCH.md Wave 0 gap):
       ```ini
       [pytest]
       testpaths = scripts/tests
       asyncio_mode = auto
       python_files = test_*.py
       python_functions = test_*
       ```
       This ensures `pytest` discovers tests correctly and pytest-asyncio runs async tests in auto mode.

    7. `scripts/tests/conftest.py`:
       - Shared pytest fixtures: `sample_raw_grant()`, `mock_httpx_client()`, `sample_registry_config(scraper_id)`
       - `mock_httpx_client` uses httpx.MockTransport for deterministic API responses
       - Import path setup: add scripts/ to sys.path
       - pytest-asyncio mode auto

    8. Package markers: `scripts/scrapers/__init__.py`, `scripts/scrapers/api/__init__.py`, `scripts/tests/__init__.py`
  </action>
  <verify>
    <automated>cd /Users/andrianthan/Desktop/projects/hanna-grants-agent && python -c "from scripts.scrapers.base_scraper import RawGrant, BaseScraper; g = RawGrant('T','F','D',None,'http://x','id'); print(g.content_hash); assert len(g.content_hash) == 64"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/scrapers/base_scraper.py` contains `class RawGrant` with `content_hash` property using `hashlib.sha256`
    - `scripts/scrapers/base_scraper.py` contains `class BaseScraper(ABC)` with `async def fetch_grants`
    - `scripts/scrapers/base_api_client.py` contains `class BaseApiClient(BaseScraper)` with `_get_json` and `_post_json`
    - `scripts/scrapers/handler.py` contains `def handler(event, context)` and `SCRAPER_CLASSES` dict
    - `scripts/scrapers/handler.py` loads `scraper_registry.json` with `json.load`
    - `infrastructure/docker/scraper/Dockerfile` contains `playwright install chromium` (NOT `playwright install`)
    - `infrastructure/docker/scraper/Dockerfile` contains `awslambdaric`
    - `scripts/scrapers/requirements.txt` contains `playwright==1.58.0` and `playwright-stealth==2.0.2`
    - `pytest.ini` exists with `testpaths = scripts/tests` and `asyncio_mode = auto`
    - `scripts/tests/conftest.py` contains `sample_raw_grant` fixture
  </acceptance_criteria>
  <done>RawGrant dataclass produces deterministic SHA-256 content hashes. BaseScraper and BaseApiClient define the inheritance hierarchy. Lambda handler dispatches to scraper classes based on registry config. Dockerfile builds a Chromium-only Playwright image with awslambdaric. pytest.ini configures test discovery and async mode. Test conftest provides shared fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 5 API parsers with unit tests</name>
  <files>
    scripts/scrapers/api/grants_ca_gov.py
    scripts/scrapers/api/grants_gov.py
    scripts/scrapers/api/propublica.py
    scripts/scrapers/api/usaspending.py
    scripts/scrapers/api/grantmakers_io.py
    scripts/tests/test_api_parsers.py
  </files>
  <read_first>
    scripts/scrapers/base_scraper.py
    scripts/scrapers/base_api_client.py
    scripts/scrapers/handler.py
    scripts/tests/conftest.py
    scraper_registry.json
    .planning/phases/02-ingestion-pipeline/02-RESEARCH.md
  </read_first>
  <behavior>
    - GrantsCaGov.fetch_grants() calls CKAN datastore_search with resource_id="111c8c88-21f6-453c-ae2c-b4785a0624f5" and returns RawGrant list
    - GrantsCaGov parses text-typed dates (ApplicationDeadline) and amounts (EstAmounts) safely -- null on parse failure, not exception
    - GrantsGov.fetch_grants() POSTs to /v1/api/search2 with eligibilities="25" (nonprofits) and paginates via startRecordNum
    - ProPublica.fetch_grants() GETs /api/v2/search.json with state="CA" filter
    - USASpending.fetch_grants() POSTs to /api/v2/search/spending_by_award/ with CA filter
    - GiantmakersIo is implemented as Playwright scraper (not API), per research Pitfall 7
    - All parsers return list[RawGrant] with source_id matching their scraper_id from registry
  </behavior>
  <action>
    Implement all 5 API/data source parsers per D-14 (OpenRouter format not relevant here -- these are grant source APIs):

    1. `scripts/scrapers/api/grants_ca_gov.py` -- Per INGEST-01, D-10:
       - `class GrantsCaGov(BaseApiClient)`
       - CKAN API: `https://data.ca.gov/api/3/action/datastore_search`
       - resource_id: `111c8c88-21f6-453c-ae2c-b4785a0624f5`
       - Pagination: limit=100, offset incrementing until records exhausted
       - Field mapping: Title -> title, AgencyDept -> funder, Description -> description, ApplicationDeadline -> deadline (parse with dateutil.parser, null on failure per Pitfall 1), EstAmounts -> extract dollar figures with regex, Geography -> geography, GrantURL -> source_url
       - Conservative pacing: 1s delay between pages per research recommendation
       - All fields are text type from CKAN -- handle with null-safe parsing throughout

    2. `scripts/scrapers/api/grants_gov.py` -- Per INGEST-01:
       - `class GrantsGov(BaseApiClient)`
       - POST to `https://api.grants.gov/v1/api/search2`
       - Body: `{"oppStatuses": "posted", "rows": 250, "startRecordNum": 0, "eligibilities": "25"}`
       - Pagination: increment startRecordNum by rows until >= hitCount per Pitfall 3
       - Field mapping: oppTitle -> title, agencyName -> funder, description -> description, closeDate -> deadline, awardCeiling/awardFloor -> funding_max/funding_min

    3. `scripts/scrapers/api/propublica.py` -- Per INGEST-01:
       - `class ProPublica(BaseApiClient)`
       - GET `https://projects.propublica.org/nonprofits/api/v2/search.json`
       - Params: state[id]=CA, page=0..N
       - 25 results per page, paginate until empty
       - Maps 990 filing data to RawGrant format (funder = organization name, description = activity summary)

    4. `scripts/scrapers/api/usaspending.py` -- Per INGEST-01:
       - `class USASpending(BaseApiClient)`
       - POST to `https://api.usaspending.gov/api/v2/search/spending_by_award/`
       - Filter by place_of_performance state=CA, award type = grants
       - Maps: recipient_name -> funder, award_description -> description, total_obligation -> funding amounts

    5. `scripts/scrapers/api/grantmakers_io.py` -- Per research Pitfall 7 (no true API):
       - `class GrantmakersIo(BaseScraper)` -- NOTE: inherits BaseScraper, NOT BaseApiClient
       - Implements as Playwright scraper since Grantmakers.io has no documented REST API
       - Navigates to https://www.grantmakers.io/, searches for CA foundations funding youth/mental health
       - Uses playwright-stealth per D-02 with random delays (2-8s)
       - Falls back gracefully if site structure changes (returns empty list, logs warning)

    6. `scripts/tests/test_api_parsers.py`:
       - Test each parser with mock HTTP responses (use httpx.MockTransport from conftest)
       - Fixtures: sample JSON responses for each API (save as inline dicts, not external files)
       - Test grants_ca_gov handles text-typed dates correctly (valid date, empty string, garbage string)
       - Test grants_gov pagination stops at hitCount
       - Test all parsers return list[RawGrant] with correct source_id
       - Test content_hash is deterministic across parser outputs

    Update `scripts/scrapers/handler.py` SCRAPER_CLASSES to include all 5 parsers.
  </action>
  <verify>
    <automated>cd /Users/andrianthan/Desktop/projects/hanna-grants-agent && python -m pytest scripts/tests/test_api_parsers.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/scrapers/api/grants_ca_gov.py` contains `resource_id` = `111c8c88-21f6-453c-ae2c-b4785a0624f5`
    - `scripts/scrapers/api/grants_ca_gov.py` contains `datastore_search` in the URL
    - `scripts/scrapers/api/grants_gov.py` contains `search2` and `eligibilities`
    - `scripts/scrapers/api/grants_gov.py` contains `startRecordNum` for pagination
    - `scripts/scrapers/api/propublica.py` contains `nonprofits/api/v2`
    - `scripts/scrapers/api/usaspending.py` contains `spending_by_award`
    - `scripts/scrapers/api/grantmakers_io.py` inherits from `BaseScraper` (not BaseApiClient)
    - `scripts/scrapers/api/grantmakers_io.py` contains `playwright` or `stealth`
    - `scripts/tests/test_api_parsers.py` has at least 5 test functions (one per parser)
    - `scripts/scrapers/handler.py` SCRAPER_CLASSES dict has 5 entries
  </acceptance_criteria>
  <done>All 5 grant source parsers return RawGrant objects with correct field mappings. Unit tests pass against mock HTTP responses. Grantmakers.io correctly implemented as Playwright scraper, not API client. Handler dispatches to all 5 parsers.</done>
</task>

</tasks>

<verification>
1. `python -c "from scripts.scrapers.base_scraper import RawGrant; g = RawGrant('T','F','D',None,'http://x','id'); assert len(g.content_hash) == 64; print('RawGrant OK')"` passes
2. `python -m pytest scripts/tests/test_api_parsers.py -x -q` passes with 5+ tests
3. `grep -r "resource_id" scripts/scrapers/api/grants_ca_gov.py` returns the CKAN resource ID
4. `grep -r "SCRAPER_CLASSES" scripts/scrapers/handler.py` shows 5 entries
5. `grep "playwright install chromium" infrastructure/docker/scraper/Dockerfile` returns a match
6. `test -f pytest.ini && grep "asyncio_mode" pytest.ini` confirms pytest config exists
</verification>

<success_criteria>
- RawGrant dataclass with SHA-256 content_hash works correctly
- All 5 API parsers instantiate and return RawGrant lists from mock data
- Docker image definition uses Chromium-only Playwright with awslambdaric
- Lambda handler dispatches to all 5 parsers based on scraper_registry.json
- pytest.ini configures test discovery and async mode for all subsequent plans
- Unit tests pass for all parsers
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-pipeline/02-01-SUMMARY.md`
</output>
