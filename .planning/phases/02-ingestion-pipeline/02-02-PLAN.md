---
phase: 02-ingestion-pipeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/scrapers/playwright/__init__.py
  - scripts/scrapers/playwright/base_playwright.py
  - scripts/scrapers/playwright/ca_dhcs.py
  - scripts/scrapers/playwright/samhsa.py
  - scripts/scrapers/playwright/bscc.py
  - scripts/scrapers/playwright/sonoma_community_foundation.py
  - scripts/scrapers/playwright/california_wellness.py
  - scripts/scrapers/playwright/blue_shield_ca.py
  - scripts/scrapers/playwright/walter_s_johnson.py
  - scripts/scrapers/playwright/sonoma_county_health.py
  - scripts/scrapers/playwright/sonoma_county_probation.py
  - scripts/scrapers/playwright/sonoma_county_oes.py
  - scripts/scrapers/playwright/sonoma_county_css.py
  - scripts/scrapers/playwright/sonoma_county_bhs.py
  - scripts/scrapers/handler.py
  - scripts/tests/test_playwright_scrapers.py
autonomous: true
requirements: [INGEST-02]

must_haves:
  truths:
    - "Each Playwright scraper uses stealth mode with random delays (2-8s) and rotating user agents"
    - "All 12 Playwright scrapers return list[RawGrant] with source_id matching scraper_registry.json"
    - "Scrapers save HTML snapshot of scraped page for debugging when grants are found"
    - "Failed scraper returns empty list and logs error — does not raise exception"
    - "Handler dispatches to all 12 Playwright scrapers plus the 5 from Plan 01"
  artifacts:
    - path: "scripts/scrapers/playwright/base_playwright.py"
      provides: "BasePlaywrightScraper with stealth, delays, user agent rotation"
      exports: ["BasePlaywrightScraper"]
    - path: "scripts/scrapers/playwright/ca_dhcs.py"
      provides: "California DHCS funding opportunities scraper"
      contains: "class CaDhcs"
    - path: "scripts/scrapers/handler.py"
      provides: "Updated handler with all 17 scraper classes"
      contains: "SCRAPER_CLASSES"
  key_links:
    - from: "scripts/scrapers/playwright/base_playwright.py"
      to: "scripts/scrapers/base_scraper.py"
      via: "class inheritance"
      pattern: "class BasePlaywrightScraper.*BaseScraper"
    - from: "scripts/scrapers/handler.py"
      to: "scripts/scrapers/playwright/"
      via: "import and SCRAPER_CLASSES dict"
      pattern: "SCRAPER_CLASSES"
---

<objective>
Implement all 12 Playwright-based scrapers with stealth mode, inheriting from a shared base class that handles browser lifecycle, anti-detection, and error handling.

Purpose: Covers the 12 non-API grant sources (CA state departments, foundations, Sonoma County portals) that require browser automation to scrape.

Output: 12 working Playwright scrapers, base class with stealth/delay/user-agent-rotation, updated handler with all 17 sources, unit tests with HTML fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ingestion-pipeline/02-CONTEXT.md
@.planning/phases/02-ingestion-pipeline/02-RESEARCH.md
@scraper_registry.json

@.planning/phases/02-ingestion-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 -->

From scripts/scrapers/base_scraper.py:
```python
@dataclass
class RawGrant:
    title: str
    funder: str
    description: str
    deadline: Optional[str]
    source_url: str
    source_id: str
    raw_html: Optional[str] = None

    @property
    def content_hash(self) -> str:
        content = f"{self.title}|{self.funder}|{self.deadline or ''}|{self.description}"
        return hashlib.sha256(content.encode()).hexdigest()

class BaseScraper(ABC):
    def __init__(self, config: dict): ...
    @abstractmethod
    async def fetch_grants(self) -> list[RawGrant]: ...
    def validate(self, grants: list[RawGrant]) -> list[RawGrant]: ...
```

From scraper_registry.json — 12 scraper-type entries:
- ca_dhcs, samhsa, bscc, sonoma_community_foundation, california_wellness, blue_shield_ca, walter_s_johnson
- sonoma_county_health, sonoma_county_probation, sonoma_county_oes, sonoma_county_css, sonoma_county_bhs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright base class and first 6 scrapers</name>
  <files>
    scripts/scrapers/playwright/__init__.py
    scripts/scrapers/playwright/base_playwright.py
    scripts/scrapers/playwright/ca_dhcs.py
    scripts/scrapers/playwright/samhsa.py
    scripts/scrapers/playwright/bscc.py
    scripts/scrapers/playwright/sonoma_community_foundation.py
    scripts/scrapers/playwright/california_wellness.py
    scripts/scrapers/playwright/blue_shield_ca.py
  </files>
  <read_first>
    scripts/scrapers/base_scraper.py
    scripts/scrapers/handler.py
    scraper_registry.json
    .planning/phases/02-ingestion-pipeline/02-RESEARCH.md
  </read_first>
  <action>
    Create Playwright scraper base and first 6 site-specific scrapers per D-02 (stealth + random delays):

    1. `scripts/scrapers/playwright/base_playwright.py`:
       - `class BasePlaywrightScraper(BaseScraper)` — inherits from base_scraper.BaseScraper
       - `async def _create_browser()`: launches chromium headless with args `["--disable-gpu", "--single-process", "--no-sandbox"]`
       - `async def _stealth_page(context)`: creates page, applies `stealth_async(page)` from playwright_stealth
       - `async def _random_delay(min_s=2.0, max_s=8.0)`: asyncio.sleep(random.uniform(min_s, max_s)) per D-02
       - `_random_user_agent()`: rotates between 3+ Chrome user agent strings per D-02
       - `_create_context(playwright)`: new_context with random user agent, viewport 1920x1080
       - `async def fetch_grants()` template method: launch browser -> create context -> stealth page -> call `_scrape_page(page)` -> close browser -> return validated grants
       - `@abstractmethod async def _scrape_page(self, page) -> list[RawGrant]`: subclasses implement this
       - Error handling: catch all exceptions in fetch_grants, log error, return empty list (never raise per D-01 silent failure approach)
       - Save page HTML via `page.content()` as raw_html on each RawGrant for debugging

    2. `scripts/scrapers/playwright/ca_dhcs.py` (California DHCS — high priority):
       - `class CaDhcs(BasePlaywrightScraper)`
       - URL: `https://www.dhcs.ca.gov/provgovpart/Pages/FundingOpportunities.aspx`
       - Navigate, wait for content load, extract funding opportunity listings
       - Map to RawGrant: title from heading, funder="California DHCS", description from listing text
       - source_id="california-dhcs" matching scraper_registry.json

    3. `scripts/scrapers/playwright/samhsa.py` (SAMHSA — high priority):
       - `class Samhsa(BasePlaywrightScraper)`
       - URL: `https://www.samhsa.gov/grants/grant-announcements`
       - Extract grant announcement listings
       - source_id="samhsa-grants"

    4. `scripts/scrapers/playwright/bscc.py` (BSCC — high priority):
       - `class Bscc(BasePlaywrightScraper)`
       - URL: `https://www.bscc.ca.gov/s_correctionsplanninggrants/`
       - source_id="bscc-ca-gov"

    5. `scripts/scrapers/playwright/sonoma_community_foundation.py` (high priority):
       - `class SonomaCommunityFoundation(BasePlaywrightScraper)`
       - URL: `https://www.sonomacf.org/grantseekers/`
       - source_id="sonoma-county-community-foundation"

    6. `scripts/scrapers/playwright/california_wellness.py` (high priority):
       - `class CaliforniaWellness(BasePlaywrightScraper)`
       - URL: `https://www.calwellness.org/grants/`
       - source_id="california-wellness-foundation"

    7. `scripts/scrapers/playwright/blue_shield_ca.py` (high priority):
       - `class BlueShieldCa(BasePlaywrightScraper)`
       - URL: `https://blueshieldcafoundation.org/grants`
       - source_id="blue-shield-ca-foundation"

    For each scraper, use CSS selectors and ARIA roles where possible (per Pitfall 5). Each `_scrape_page` method should:
    - Wait for main content to load (page.wait_for_selector or page.wait_for_load_state)
    - Add random delay between navigation actions per D-02
    - Extract grant listings into RawGrant objects
    - Set source_id to match scraper_registry.json exactly
    - Return empty list if no grants found (not an error — some sites have seasonal postings)
  </action>
  <verify>
    <automated>cd /Users/andrianthan/Desktop/projects/hanna-grants-agent && python -c "from scripts.scrapers.playwright.base_playwright import BasePlaywrightScraper; print('BasePlaywrightScraper imported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/scrapers/playwright/base_playwright.py` contains `class BasePlaywrightScraper(BaseScraper)`
    - `scripts/scrapers/playwright/base_playwright.py` contains `stealth_async` import from playwright_stealth
    - `scripts/scrapers/playwright/base_playwright.py` contains `random.uniform` for delays between 2.0 and 8.0
    - `scripts/scrapers/playwright/base_playwright.py` has `_random_user_agent` method with 3+ user agent strings
    - `scripts/scrapers/playwright/base_playwright.py` wraps fetch_grants in try/except returning empty list on error
    - Each of the 6 scraper files contains a class inheriting from `BasePlaywrightScraper`
    - Each scraper's source_id matches the scraper_id in scraper_registry.json
    - `scripts/scrapers/playwright/ca_dhcs.py` contains `class CaDhcs(BasePlaywrightScraper)` with `source_id` or matching scraper_id
  </acceptance_criteria>
  <done>BasePlaywrightScraper provides stealth mode, random delays, user agent rotation, and error-safe fetch_grants. 6 high-priority Playwright scrapers implemented with correct source_ids matching registry.</done>
</task>

<task type="auto">
  <name>Task 2: Implement remaining 6 scrapers, update handler, add tests</name>
  <files>
    scripts/scrapers/playwright/walter_s_johnson.py
    scripts/scrapers/playwright/sonoma_county_health.py
    scripts/scrapers/playwright/sonoma_county_probation.py
    scripts/scrapers/playwright/sonoma_county_oes.py
    scripts/scrapers/playwright/sonoma_county_css.py
    scripts/scrapers/playwright/sonoma_county_bhs.py
    scripts/scrapers/handler.py
    scripts/tests/test_playwright_scrapers.py
  </files>
  <read_first>
    scripts/scrapers/playwright/base_playwright.py
    scripts/scrapers/playwright/ca_dhcs.py
    scripts/scrapers/handler.py
    scripts/tests/conftest.py
    scraper_registry.json
  </read_first>
  <action>
    Implement remaining 6 Playwright scrapers and wire everything together:

    1. `scripts/scrapers/playwright/walter_s_johnson.py`:
       - `class WalterSJohnson(BasePlaywrightScraper)`
       - URL: `https://www.wsjf.org/how-we-work/`
       - source_id="walter-s-johnson-foundation"

    2. `scripts/scrapers/playwright/sonoma_county_health.py`:
       - `class SonomaCountyHealth(BasePlaywrightScraper)`
       - URL: `https://sonomacounty.ca.gov/health-and-human-services/health-services`
       - source_id="sonoma-county-health"

    3. `scripts/scrapers/playwright/sonoma_county_probation.py`:
       - `class SonomaCountyProbation(BasePlaywrightScraper)`
       - URL: `https://sonomacounty.ca.gov/probation`
       - source_id="sonoma-county-probation"

    4. `scripts/scrapers/playwright/sonoma_county_oes.py`:
       - `class SonomaCountyOes(BasePlaywrightScraper)`
       - URL: `https://sonomacounty.ca.gov/oes`
       - source_id="sonoma-county-oes"

    5. `scripts/scrapers/playwright/sonoma_county_css.py`:
       - `class SonomaCountyCss(BasePlaywrightScraper)`
       - URL: `https://sonomacounty.ca.gov/css`
       - source_id="sonoma-county-css"

    6. `scripts/scrapers/playwright/sonoma_county_bhs.py`:
       - `class SonomaCountyBhs(BasePlaywrightScraper)`
       - URL: `https://sonomacounty.ca.gov/bhd`
       - source_id="sonoma-county-bhs"

    7. Update `scripts/scrapers/handler.py`:
       - Add all 12 Playwright scrapers to SCRAPER_CLASSES dict (+ existing 5 API parsers = 17 total)
       - Import all 12 playwright classes and the 5 from Plan 01

    8. `scripts/tests/test_playwright_scrapers.py`:
       - Test BasePlaywrightScraper._random_user_agent returns valid Chrome UA strings
       - Test BasePlaywrightScraper._random_delay range (mock asyncio.sleep to verify bounds)
       - Test that each scraper class can be instantiated with registry config
       - Test that handler.SCRAPER_CLASSES has exactly 17 entries matching scraper_registry.json scraper_ids
       - Mock Playwright to verify stealth_async is called on page creation
  </action>
  <verify>
    <automated>cd /Users/andrianthan/Desktop/projects/hanna-grants-agent && python -c "from scripts.scrapers.handler import SCRAPER_CLASSES; assert len(SCRAPER_CLASSES) == 17, f'Expected 17, got {len(SCRAPER_CLASSES)}'; print(f'All {len(SCRAPER_CLASSES)} scrapers registered')"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 remaining scraper files exist with correct class names inheriting BasePlaywrightScraper
    - `scripts/scrapers/handler.py` SCRAPER_CLASSES dict has exactly 17 entries
    - Every scraper_id in scraper_registry.json has a corresponding entry in SCRAPER_CLASSES
    - `scripts/tests/test_playwright_scrapers.py` has at least 4 test functions
    - Each Sonoma County scraper's source_id matches the scraper_id from scraper_registry.json (e.g., "sonoma-county-bhs" not "sonoma_county_bhs")
  </acceptance_criteria>
  <done>All 12 Playwright scrapers implemented. Handler dispatches to all 17 sources (5 API + 12 Playwright). Tests verify correct registration and base class behavior.</done>
</task>

</tasks>

<verification>
1. `python -c "from scripts.scrapers.handler import SCRAPER_CLASSES; assert len(SCRAPER_CLASSES) == 17"` passes
2. `python -m pytest scripts/tests/test_playwright_scrapers.py -x -q` passes
3. `grep -r "stealth_async" scripts/scrapers/playwright/base_playwright.py` returns a match
4. `grep -c "BasePlaywrightScraper" scripts/scrapers/playwright/*.py` shows 12+ matches (base + 12 scrapers)
</verification>

<success_criteria>
- All 12 Playwright scrapers implemented with stealth mode and random delays per D-02
- Handler SCRAPER_CLASSES has exactly 17 entries matching all scraper_ids in registry
- BasePlaywrightScraper error handling returns empty list on failure (never raises)
- Tests pass for base class behavior and scraper registration
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-pipeline/02-02-SUMMARY.md`
</output>
